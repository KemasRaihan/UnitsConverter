public interface WeightConverter {
    public float convertToGrams(float amount);
    public float convertToKilograms(float amount);
    public float convertToMilligrams(float amount);
    public float convertToPounds(float amount);
}
